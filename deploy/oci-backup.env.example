# Copy to deploy/oci-backup.env on the VPS and replace all placeholder values locally.
OCI_REGION=replace_with_oci_region
OCI_NAMESPACE=replace_with_object_storage_namespace
OCI_BUCKET=replace_with_backup_bucket
OCI_BACKUP_WORK_ROOT=/var/tmp/medical-bot-oci-backups
